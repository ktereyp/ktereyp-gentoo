#ifndef ${UPTARGET}_VERSION_H
#define ${UPTARGET}_VERSION_H

#define ${UPTARGET}_VERSION_MAJOR ${VERSION_MAJOR}
#define ${UPTARGET}_VERSION_MINOR ${VERSION_MINOR}
#define ${UPTARGET}_VERSION_PATCH ${VERSION_PATCH}
#define ${UPTARGET}_VERSION_NUM ${VERSION_NUM}
#define ${UPTARGET}_VERSION_STR "${VERSION}"
#define ${UPTARGET}_VERSION ${UPTARGET}_VERSION_NUM

#endif
